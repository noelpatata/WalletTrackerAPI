meta {
  name: register
  type: http
  seq: 1
}

post {
  url: {{base_url}}register
  body: json
  auth: inherit
}

body:json {
  {
    "username":"admin",
    "password":"adminadmin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
