meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{base_url}}login
  body: json
  auth: inherit
}

body:json {
  {
    "username":"admin",
    "password":"adminadmin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
