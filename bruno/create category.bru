meta {
  name: create category
  type: http
  seq: 3
}

post {
  url: {{base_url}}ExpenseCategory
  body: json
  auth: bearer
}

headers {
  Signature: {{signature}}
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name":"Comida"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
